<% provide(:title, 'Login to Lemonade Ledger') %>

<h1>Login to Lemonade Ledger</h1>

<%= form_tag('/login', method:'post') do %>
  <label for="password">Password:</label>
  <input type="password" name="password" class="form_field"/>
  <input type="submit" name="commit" value="Authenticate this device" class="form_field"/>
<% end %>
<p>
  (this only needs to be done once per device)
</p>
